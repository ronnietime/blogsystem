<section class="selected-tag"><%=@category.title.html_safe %></section>

<div class="main-wrapper">
  <section class="main">
    <% if @posts.any? %>
      <%= render :partial => "/refinery/blog/shared/post", :collection => @posts %>
      <%= will_paginate @posts %>
    <% end %>
  </section>
</div>

<%= render "/refinery/content_page", :hide_sections => :body_content_title %>
